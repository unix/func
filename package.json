{
  "name": "func-workspace",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "benchmarks": "pnpm --filter func-cli-benchmarks report",
    "benchmarks:test": "pnpm --filter func-cli-benchmarks test",
    "test": "pnpm --filter func test",
    "docs:dev": "pnpm --filter docs dev"
  },
  "repository": {
    "type": "git",
    "url": "http://github.com/unix/func"
  },
  "prettier": "@unix/prettier",
  "devDependencies": {
    "@unix/prettier": "^0.1.0",
    "prettier": "^3.8.3"
  }
}
