# create-func

Create a new func project from the `func-template` package.

```bash
npm init func@latest
```

Create a project without an interactive prompt:

```bash
npm init func@latest my-app
```

Run the real-project end-to-end test:

```bash
pnpm test:e2e
```
